function convert_to_inches (length int)
{
	return length*12;
}
%%
function conver_to_feet (length int)
{
	return length/12;
}
%%
int length;
string unit;
get (lenght, unit);
if (unit == feet)
{
	put(convert_to_inches(lenght));
}
otherwise
$
{
	put(convert_to_feet(length));
}
fi
%%
